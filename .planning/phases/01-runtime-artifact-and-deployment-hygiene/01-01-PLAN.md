---
phase: 01-runtime-artifact-and-deployment-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .dockerignore
  - README.md
  - docs/raspberry-pi-deployment.md
autonomous: true
user_setup: []
requirements:
  - REQ-001
  - REQ-003
  - REQ-004
  - REQ-010
must_haves:
  truths:
    - "Git ignores SQLite database runtime sidecars created beside local databases."
    - "Docker build context excludes the same SQLite runtime sidecars so local data is not copied toward images."
    - "README and Raspberry Pi deployment docs state that APScheduler is in-process and deployment must use exactly one Gunicorn worker and one app instance per shared data directory."
    - "Deployment docs consistently describe host ./data mounted to container /data with Docker Compose setting DATA_DIR=/data."
    - "No Flask, SQLite, APScheduler, Docker command, Compose runtime, or DATA_DIR code behavior changes are made for this hygiene phase."
  artifacts:
    - path: ".gitignore"
      provides: "Git exclusions for local database files and SQLite sidecars"
      contains:
        - "*.db-wal"
        - "*.db-shm"
        - "*.sqlite-wal"
        - "*.sqlite-shm"
        - "*.sqlite3-wal"
        - "*.sqlite3-shm"
    - path: ".dockerignore"
      provides: "Docker build-context exclusions for local database files and SQLite sidecars"
      contains:
        - "*.db-wal"
        - "*.db-shm"
        - "*.sqlite-wal"
        - "*.sqlite-shm"
        - "*.sqlite3-wal"
        - "*.sqlite3-shm"
    - path: "README.md"
      provides: "Top-level deployment and runtime artifact guidance"
      contains: "APScheduler"
    - path: "docs/raspberry-pi-deployment.md"
      provides: "Raspberry Pi deployment guidance for one-worker operation, DATA_DIR, backup, and restore"
      contains: "DATA_DIR"
  key_links:
    - from: "SQLite WAL mode runtime files"
      to: ".gitignore"
      via: "explicit *-wal and *-shm ignore patterns"
      pattern: "\\*\\.db-(wal|shm)|\\*\\.sqlite(3)?-(wal|shm)"
    - from: "SQLite WAL mode runtime files"
      to: ".dockerignore"
      via: "matching Docker build-context ignore patterns"
      pattern: "\\*\\.db-(wal|shm)|\\*\\.sqlite(3)?-(wal|shm)"
    - from: "Dockerfile and docker-compose.yml"
      to: "README.md and docs/raspberry-pi-deployment.md"
      via: "documented one-worker Gunicorn and DATA_DIR=/data assumptions"
      pattern: "gunicorn.*-w 1|DATA_DIR=/data|DATA_DIR: /data"
---

<objective>
Keep local/runtime files out of Git and make the Raspberry Pi deployment assumptions explicit and enforceable.

Purpose: Prevent SQLite WAL/SHM sidecars and related local runtime files from appearing as source changes, while preserving the current Flask, SQLite, APScheduler, Docker Compose, and Raspberry Pi deployment shape.
Output: Updated ignore rules and deployment documentation. No runtime behavior changes are expected.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@./AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-artifact-and-deployment-hygiene/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.gitignore
@.dockerignore
@README.md
@docs/raspberry-pi-deployment.md
@Dockerfile
@docker-compose.yml

Planning notes:
- `CODEX.md` was not present during planning.
- No project-local `.agents/skills/` files were present during planning.
- The phase research found the expected current deployment shape already in place: Docker Compose sets `DATA_DIR=/data`, mounts host `./data` to container `/data`, and runs Gunicorn with one worker and four threads.
- Keep this plan conservative. Do not change runtime code unless command verification reveals a direct inconsistency that blocks the phase goal.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ignore SQLite WAL and SHM runtime sidecars</name>
  <files>.gitignore, .dockerignore</files>
  <action>
    Update the local database and runtime artifact sections in `.gitignore` and `.dockerignore`.

    Add explicit SQLite sidecar patterns beside the existing `*.db`, `*.sqlite`, and `*.sqlite3` entries:
    - `*.db-wal`
    - `*.db-shm`
    - `*.sqlite-wal`
    - `*.sqlite-shm`
    - `*.sqlite3-wal`
    - `*.sqlite3-shm`

    Also add rollback-journal sidecar patterns only if they fit cleanly in the same local database section:
    - `*.db-journal`
    - `*.sqlite-journal`
    - `*.sqlite3-journal`

    Preserve the existing exclusions for `.env`, databases, backups, logs, `data/`, uploads, Excel files, PDFs, Python caches, virtual environments, and editor folders. Do not add broad source-code, dependency, or build-output exclusions unrelated to this phase.
  </action>
  <verify>
    Run `git check-ignore -v monitoring_board.db monitoring_board.db-wal monitoring_board.db-shm monitoring_board.db-journal monitoring_board.sqlite-wal monitoring_board.sqlite-shm monitoring_board.sqlite3-wal monitoring_board.sqlite3-shm`.

    Run `Select-String -Path .dockerignore -Pattern '\*.db-wal','\*.db-shm','\*.sqlite-wal','\*.sqlite-shm','\*.sqlite3-wal','\*.sqlite3-shm'`.
  </verify>
  <done>
    Root-level SQLite WAL and SHM sidecars for `.db`, `.sqlite`, and `.sqlite3` database names are ignored by Git and excluded from Docker build context. Existing runtime artifact exclusions remain intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten Raspberry Pi deployment documentation</name>
  <files>README.md, docs/raspberry-pi-deployment.md</files>
  <action>
    Review the existing Docker and Raspberry Pi sections in `README.md` and `docs/raspberry-pi-deployment.md`, then make small documentation edits that reinforce the current deployment assumptions:
    - State that APScheduler runs inside the Flask/Gunicorn process, so production must keep exactly one Gunicorn worker while that remains true.
    - State that Gunicorn threads are acceptable for this deployment because they remain inside one worker process.
    - Warn against increasing worker count, setting worker-count environment shortcuts, running `docker compose up --scale monitoring-board=2`, or starting a second app instance against the same `./data` unless scheduling is redesigned.
    - Keep the Docker Compose path story explicit: host `./data` is mounted into the container as `/data`; Compose sets `DATA_DIR=/data`; host-side backup and restore commands use `DATA_DIR=./data`.
    - Add SQLite sidecars such as `monitoring_board.db-wal` and `monitoring_board.db-shm` to the local-files guidance.
    - Preserve the current Cloudflare Tunnel, SQLite, Docker Compose, and one-worker deployment model. Do not introduce Celery, Redis, Postgres, Kubernetes, new backup infrastructure, or runtime code changes.
  </action>
  <verify>
    Run `Select-String -Path README.md,docs/raspberry-pi-deployment.md -Pattern 'APScheduler','DATA_DIR','/data','./data','--scale','db-wal','db-shm'`.

    Run `Select-String -Path Dockerfile,docker-compose.yml -Pattern 'gunicorn.*-w 1','DATA_DIR: /data','./data:/data'`.
  </verify>
  <done>
    README and Raspberry Pi deployment guide explicitly document the one-worker APScheduler constraint, avoid multi-process or Compose scaling guidance, and describe `DATA_DIR` host/container paths consistently.
  </done>
</task>

</tasks>

<verification>
Run these checks after both tasks:

1. `git check-ignore -v monitoring_board.db monitoring_board.db-wal monitoring_board.db-shm monitoring_board.sqlite-wal monitoring_board.sqlite-shm monitoring_board.sqlite3-wal monitoring_board.sqlite3-shm`
2. `Select-String -Path .dockerignore -Pattern '\*.db-wal','\*.db-shm','\*.sqlite-wal','\*.sqlite-shm','\*.sqlite3-wal','\*.sqlite3-shm'`
3. `Select-String -Path README.md,docs/raspberry-pi-deployment.md -Pattern 'APScheduler','DATA_DIR','/data','./data','--scale','db-wal','db-shm'`
4. `Select-String -Path Dockerfile,docker-compose.yml -Pattern 'gunicorn.*-w 1','DATA_DIR: /data','./data:/data'`
5. `python -m pytest -q tests/test_runtime_paths.py`
6. Confirm `git diff -- Dockerfile docker-compose.yml app.py monitoring_board/runtime.py` is empty. If it is not empty, document why a behavior change was necessary and ensure tests cover it.
</verification>

<success_criteria>
- `.gitignore` ignores SQLite WAL and SHM sidecars for `.db`, `.sqlite`, and `.sqlite3` files.
- `.dockerignore` excludes the same SQLite sidecars from Docker build context.
- README and Raspberry Pi deployment guide warn against multiple Gunicorn workers, Compose scaling, and a second app instance sharing the same runtime data while APScheduler is in-process.
- README and Raspberry Pi deployment guide consistently distinguish host `./data`, container `/data`, Compose `DATA_DIR=/data`, and host backup `DATA_DIR=./data`.
- No runtime code, Docker command, or Docker Compose behavior changes are made unless verification reveals a direct inconsistency.
- Existing runtime path tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-artifact-and-deployment-hygiene/01-01-SUMMARY.md` with the files changed, verification commands run, and any skipped tests or behavior-change decisions.
</output>
