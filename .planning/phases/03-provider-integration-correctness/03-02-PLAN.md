---
phase: 03-provider-integration-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/sigenergy/auth_success_json_string.json
  - tests/fixtures/sigenergy/auth_success_object.json
  - tests/fixtures/sigenergy/systems_list.json
  - tests/fixtures/sigenergy/realtime_data.json
  - tests/fixtures/sigenergy/energy_flow.json
  - tests/fixtures/sigenergy/error_code_payload.json
  - tests/test_sigenergy_provider_contracts.py
autonomous: true
requirements:
  - REQ-005
  - REQ-006
must_haves:
  truths:
    - "Sigenergy token, system-list, realtime, energy-flow, and error response shapes are pinned in local fixtures."
    - "Sigenergy JSON-string data payloads, object payloads, status mapping, and energy-flow notes have focused regression coverage."
    - "Sigenergy provider errors continue to surface through the existing integration sync failure persistence path."
  artifacts:
    - path: "tests/fixtures/sigenergy/auth_success_json_string.json"
      provides: "Pinned Sigenergy auth packet where data is a JSON-encoded string"
      contains: "accessToken"
    - path: "tests/fixtures/sigenergy/systems_list.json"
      provides: "Pinned Sigenergy systems list shape"
      contains: "systemId"
    - path: "tests/fixtures/sigenergy/energy_flow.json"
      provides: "Pinned Sigenergy energy-flow values used for notes/status inference"
      contains: "pvPower"
    - path: "tests/test_sigenergy_provider_contracts.py"
      provides: "Regression tests for Sigenergy fixture parsing and sync error visibility"
      min_lines: 100
  key_links:
    - from: "tests/test_sigenergy_provider_contracts.py"
      to: "app.py"
      via: "imports Sigenergy parser/token/status helpers"
      pattern: "parse_provider_payload_data"
    - from: "tests/test_sigenergy_provider_contracts.py"
      to: "app.py"
      via: "integration sync failure persistence assertion"
      pattern: "run_integration_sync"
    - from: "tests/fixtures/sigenergy/auth_success_json_string.json"
      to: "app.py"
      via: "get_sigenergy_token parses JSON-string data"
      pattern: "accessToken"
---

<objective>
Add fixture-backed Sigenergy provider contract coverage for token parsing, system/realtime/energy-flow normalization, and provider error persistence.

Purpose: Sigenergy support is newer and less covered than FusionSolar. Phase 3 should pin known response shapes and document current limits without adding infrastructure or broad integration rewrites.
Output: Local Sigenergy JSON fixtures plus focused pytest coverage around existing parsing, token, normalization, and sync-error seams.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-provider-integration-correctness/03-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/02-background-job-and-scheduler-safety/02-01-SUMMARY.md
@app.py
@tests/test_scheduler_safety.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin realistic Sigenergy response fixtures</name>
  <files>tests/fixtures/sigenergy/auth_success_json_string.json, tests/fixtures/sigenergy/auth_success_object.json, tests/fixtures/sigenergy/systems_list.json, tests/fixtures/sigenergy/realtime_data.json, tests/fixtures/sigenergy/energy_flow.json, tests/fixtures/sigenergy/error_code_payload.json</files>
  <action>
    Create `tests/fixtures/sigenergy/` and add small, fake JSON packets that represent the Sigenergy API shapes already handled by the code. Keep all values fake and non-secret.

    Include these fixture shapes:
    - `auth_success_json_string.json`: `code=0`, `msg` success text, and `data` as a JSON-encoded string containing `accessToken` and `expiresIn`.
    - `auth_success_object.json`: the same token fields with `data` as an object.
    - `systems_list.json`: a successful response with `data.list` containing at least two systems with `systemId` and `systemName`.
    - `realtime_data.json`: a successful response with `data` containing `systemStatus` or another current status variant.
    - `energy_flow.json`: a successful response with `data` containing `pvPower`, `gridPower`, `batteryPower`, `batterySoc`, and `loadPower` variants used by `normalize_sigenergy_system_row()`.
    - `error_code_payload.json`: a non-zero provider-code payload that can represent throttling or token/permission failure without asserting an unknown official code contract.

    Use ASCII JSON content and stable fake IDs such as `SIG-001`. Do not add real app keys, secrets, tokens, customer names, or live API packets.
  </action>
  <verify>python -m json.tool tests/fixtures/sigenergy/auth_success_json_string.json; python -m json.tool tests/fixtures/sigenergy/auth_success_object.json; python -m json.tool tests/fixtures/sigenergy/systems_list.json; python -m json.tool tests/fixtures/sigenergy/realtime_data.json; python -m json.tool tests/fixtures/sigenergy/energy_flow.json; python -m json.tool tests/fixtures/sigenergy/error_code_payload.json</verify>
  <done>All Sigenergy fixture files are valid JSON, contain the expected provider response keys, and avoid live provider/customer data.</done>
</task>

<task type="auto">
  <name>Task 2: Assert Sigenergy parsing, status mapping, and error persistence</name>
  <files>tests/test_sigenergy_provider_contracts.py</files>
  <action>
    Add `tests/test_sigenergy_provider_contracts.py` using `from __future__ import annotations`, `pathlib.Path`, `json`, `pytest`, and direct imports from `app.py`.

    Cover these behaviors using the new fixtures:
    - `parse_provider_payload_data()` parses JSON-string `data`, returns object `data` unchanged, and preserves invalid strings as strings.
    - `get_sigenergy_token()` accepts both JSON-string and object token payloads when `requests.post` is monkeypatched with a small fake response. Clear `SIGENERGY_TOKEN_CACHE` before/after token tests so cache state does not leak between tests.
    - `normalize_sigenergy_system_rows()` accepts `data.list`, common list variants, and a single-system object.
    - `normalize_sigenergy_system_row()` maps fixture realtime/energy-flow fields into `external_id`, `external_name`, `status`, `raw_status`, `notes`, and `payload`, including `pvPower`/`batterySoc` notes.
    - `fetch_sigenergy_json()` raises `ValueError` on non-zero provider `code` using `error_code_payload.json`.
    - `run_integration_sync()` persists a Sigenergy provider failure through `integration_sync_runs.status='error'` and `integration_configs.last_error` when `run_provider_check()` is monkeypatched to raise the non-zero-code error. Use `tmp_path`, `ensure_database()`, and `get_db()`; do not call real Sigenergy endpoints.

    Keep implementation changes out of this plan unless a fixture-backed test exposes a clear correctness bug in the touched Sigenergy helper. If a behavior change is necessary, keep it to the smallest affected helper and document it as an auto-fixed deviation in the summary.
  </action>
  <verify>python -m pytest -q tests/test_sigenergy_provider_contracts.py tests/test_scheduler_safety.py</verify>
  <done>Sigenergy provider fixture tests pass and explicitly protect JSON-string payload parsing, token extraction, status/notes normalization, and existing sync error persistence.</done>
</task>

</tasks>

<verification>
Run `python -m pytest -q tests/test_sigenergy_provider_contracts.py tests/test_scheduler_safety.py`.
</verification>

<success_criteria>
- Sigenergy fixture files exist for auth, systems, realtime, energy-flow, and provider-error response shapes.
- No tests perform live Sigenergy HTTP calls.
- Fixture-backed tests cover JSON-string `data`, object `data`, token extraction, status mapping, energy-flow notes, non-zero provider errors, and sync error persistence.
- Existing scheduler/provider-neutral dispatch tests continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-provider-integration-correctness/03-02-SUMMARY.md`.
</output>
