---
phase: 03-provider-integration-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/fusionsolar/stations_page_1.json
  - tests/fixtures/fusionsolar/realtime_kpi.json
  - tests/fixtures/fusionsolar/alarms_active.json
  - tests/fixtures/fusionsolar/kpi_day_rows.json
  - tests/fixtures/fusionsolar/kpi_month_rows.json
  - tests/test_fusionsolar_provider_contracts.py
autonomous: true
requirements:
  - REQ-005
  - REQ-006
  - REQ-011
must_haves:
  truths:
    - "FusionSolar station, realtime, alarm, and KPI response shapes are pinned in realistic local fixtures."
    - "FusionSolar status mapping, alarm severity handling, timestamp parsing, production key priority, and failCode detection have focused regression coverage."
    - "FusionSolar production fixture tests make the selected kWh source and raw selected key explicit."
  artifacts:
    - path: "tests/fixtures/fusionsolar/stations_page_1.json"
      provides: "Pinned FusionSolar station-list packet with data.list pagination shape"
      contains: "plantCode"
    - path: "tests/fixtures/fusionsolar/realtime_kpi.json"
      provides: "Pinned FusionSolar realtime KPI packet with dataItemMap health state"
      contains: "real_health_state"
    - path: "tests/fixtures/fusionsolar/kpi_day_rows.json"
      provides: "Pinned FusionSolar daily KPI rows with collectTime and production keys"
      contains: "PVYield"
    - path: "tests/test_fusionsolar_provider_contracts.py"
      provides: "Regression tests for FusionSolar fixture parsing and production assumptions"
      min_lines: 100
  key_links:
    - from: "tests/test_fusionsolar_provider_contracts.py"
      to: "app.py"
      via: "imports fetch/normalize/date/production/error helpers"
      pattern: "parse_fusionsolar_collect_date"
    - from: "tests/test_fusionsolar_provider_contracts.py"
      to: "monitoring_board/services/fusionsolar.py"
      via: "status mapping assertions"
      pattern: "map_fusionsolar_status"
    - from: "tests/fixtures/fusionsolar/kpi_day_rows.json"
      to: "app.py"
      via: "select_production_value fixture assertions"
      pattern: "PVYield"
---

<objective>
Add fixture-backed FusionSolar provider contract coverage for response parsing, timestamps, production units, and rate-limit/session-expiry recognition.

Purpose: Phase 3 needs provider correctness confidence before any broader integration behavior changes. FusionSolar is the highest-risk path for production records and customer-facing reporting.
Output: Local FusionSolar JSON fixtures plus focused pytest coverage around existing parsing and normalization seams.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-provider-integration-correctness/03-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/02-background-job-and-scheduler-safety/02-01-SUMMARY.md
@app.py
@monitoring_board/services/fusionsolar.py
@tests/test_fusionsolar_service.py
@tests/test_fusionsolar_sync.py
@tests/test_performance.py
@tests/test_performance_backfill.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin realistic FusionSolar response fixtures</name>
  <files>tests/fixtures/fusionsolar/stations_page_1.json, tests/fixtures/fusionsolar/realtime_kpi.json, tests/fixtures/fusionsolar/alarms_active.json, tests/fixtures/fusionsolar/kpi_day_rows.json, tests/fixtures/fusionsolar/kpi_month_rows.json</files>
  <action>
    Create `tests/fixtures/fusionsolar/` and add small, reviewable JSON packets that look like FusionSolar Northbound API responses. Keep values fake and non-secret.

    Include these fixture shapes:
    - `stations_page_1.json`: `success=true`, `failCode=0`, `data.pageCount=1`, and `data.list` with at least two station rows using `plantCode`/`plantName` and one row using `stationCode`/`stationName` or another observed variant.
    - `realtime_kpi.json`: `data` list rows keyed by station code with `dataItemMap.real_health_state` values covering healthy, faulty, and disconnected status mapping.
    - `alarms_active.json`: active alarm rows with `stationCode`, `lev`, and `alarmName`, covering at least a minor alarm and a major/critical alarm.
    - `kpi_day_rows.json`: daily KPI rows with millisecond `collectTime`, `stationCode`, and `dataItemMap` values for `PVYield`, `inverterYield`, `inverter_power`, plus non-production keys such as `selfUsePower` or `ongrid_power`.
    - `kpi_month_rows.json`: monthly KPI rows with month-level `collectTime` and the same production key families.

    Use ASCII JSON content and stable dates such as 2026-01-15 and 2026-01-01. Do not include live provider payloads, account names, credentials, or real station IDs.
  </action>
  <verify>python -m json.tool tests/fixtures/fusionsolar/stations_page_1.json; python -m json.tool tests/fixtures/fusionsolar/realtime_kpi.json; python -m json.tool tests/fixtures/fusionsolar/alarms_active.json; python -m json.tool tests/fixtures/fusionsolar/kpi_day_rows.json; python -m json.tool tests/fixtures/fusionsolar/kpi_month_rows.json</verify>
  <done>All FusionSolar fixture files are valid JSON, contain the expected provider response keys, and are small enough to be reviewed as provider contract examples.</done>
</task>

<task type="auto">
  <name>Task 2: Assert FusionSolar parsing and production assumptions from fixtures</name>
  <files>tests/test_fusionsolar_provider_contracts.py</files>
  <action>
    Add `tests/test_fusionsolar_provider_contracts.py` using `from __future__ import annotations`, `pathlib.Path`, `json`, and direct imports from `app.py` and `monitoring_board.services.fusionsolar`.

    Cover these behaviors using the new fixtures:
    - Station, realtime, alarm, and KPI rows can be loaded and normalized through existing helpers such as `fetch_fusionsolar_stations()`, `fetch_fusionsolar_realtime_map()`, `fetch_fusionsolar_alarm_map()`, `normalize_fusionsolar_kpi_rows()`, and `normalize_fusionsolar_plant_row()` by using small fake session/response objects. Do not call a real API.
    - `map_fusionsolar_status()` and `normalize_fusionsolar_plant_row()` map healthy rows to `Operacional`, disconnected rows to `Desconectada`, minor alarms to `Alerta`, and major/critical alarms to `Erro`.
    - `parse_fusionsolar_collect_date()` handles fixture millisecond timestamps and string/date fallback values explicitly. Assert the current local-time conversion behavior; do not change timezone semantics unless a clear bug is proven.
    - `select_production_value()` prioritizes `PVYield`, then `inverterYield`, then `inverter_power`, and returns both selected key and selected raw value from fixture `dataItemMap` values.
    - `post_fusionsolar_json()` raises a `ValueError` that existing `is_fusionsolar_rate_limit_error()` and `is_fusionsolar_session_expired_error()` recognize for `failCode=407` and `failCode=305` payloads. Cover int and string `failCode` variants if concise.

    Keep implementation changes out of this plan unless a fixture-backed test exposes a clear correctness bug in the touched helper. If a behavior change is necessary, keep it to the smallest affected FusionSolar helper and document it as an auto-fixed deviation in the summary.
  </action>
  <verify>python -m pytest -q tests/test_fusionsolar_provider_contracts.py tests/test_fusionsolar_service.py tests/test_performance.py tests/test_performance_backfill.py</verify>
  <done>FusionSolar provider fixture tests pass and explicitly protect status mapping, production key selection, timestamp parsing, and rate-limit/session-expiry detection.</done>
</task>

</tasks>

<verification>
Run `python -m pytest -q tests/test_fusionsolar_provider_contracts.py tests/test_fusionsolar_service.py tests/test_fusionsolar_sync.py tests/test_performance.py tests/test_performance_backfill.py`.
</verification>

<success_criteria>
- FusionSolar fixture files exist for station list, realtime KPI, alarms, daily KPI, and monthly KPI response shapes.
- No tests perform live FusionSolar HTTP calls.
- Fixture-backed tests cover status mapping, alarm severity, timestamp conversion, production key priority, raw selected production values, rate-limit detection, and session-expiry detection.
- Existing FusionSolar sync and production tests continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-provider-integration-correctness/03-01-SUMMARY.md`.
</output>
