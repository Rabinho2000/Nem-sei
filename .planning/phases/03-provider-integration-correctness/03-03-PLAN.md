---
phase: 03-provider-integration-correctness
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - .planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md
autonomous: true
requirements:
  - REQ-005
  - REQ-006
  - REQ-011
must_haves:
  truths:
    - "Remaining FusionSolar and Sigenergy provider assumptions are documented after fixture-backed tests exist."
    - "Documented assumptions distinguish pinned behavior from unresolved provider contracts requiring real account samples."
    - "The phase ends with targeted provider regression commands listed in a planning note."
  artifacts:
    - path: ".planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md"
      provides: "Provider assumptions note tied to the Phase 3 fixture tests"
      contains: "Remaining assumptions"
  key_links:
    - from: ".planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md"
      to: "tests/fixtures/fusionsolar"
      via: "documents which FusionSolar response shapes are pinned"
      pattern: "FusionSolar"
    - from: ".planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md"
      to: "tests/fixtures/sigenergy"
      via: "documents which Sigenergy response shapes are pinned"
      pattern: "Sigenergy"
    - from: ".planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md"
      to: "tests/test_fusionsolar_provider_contracts.py"
      via: "references provider regression commands"
      pattern: "test_fusionsolar_provider_contracts.py"
---

<objective>
Document the provider assumptions that remain after FusionSolar and Sigenergy fixture-backed regression coverage is in place.

Purpose: Phase 3 should reduce risk without pretending unknown provider contracts are fully proven. The final note gives future work a clear boundary between tested local behavior and assumptions that need live-provider samples.
Output: A concise Phase 3 provider assumptions note with pinned behaviors, unresolved assumptions, and regression commands.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-provider-integration-correctness/03-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/03-provider-integration-correctness/03-01-SUMMARY.md
@.planning/phases/03-provider-integration-correctness/03-02-SUMMARY.md
@tests/test_fusionsolar_provider_contracts.py
@tests/test_sigenergy_provider_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 3 provider assumptions note</name>
  <files>.planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md</files>
  <action>
    Create `.planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md` after Plans 03-01 and 03-02 have summaries. Keep it concise and operational.

    Include these sections:
    - `# Phase 3 Provider Assumptions`
    - `## Pinned By Fixtures`: list the FusionSolar and Sigenergy fixture files and the behaviors now covered by tests.
    - `## Remaining Assumptions`: explicitly document unresolved assumptions for FusionSolar `collectTime` timezone semantics, FusionSolar production units/key priority (`PVYield`, `inverterYield`, `inverter_power` treated as kWh), FusionSolar failCode string matching, Sigenergy rate-limit/token-expiry codes, Sigenergy energy-flow freshness/rate limits, and configured-system fallback behavior.
    - `## Do Not Rely On Yet`: list provider claims that still need real account samples before expansion, such as exact Sigenergy throttle codes and authoritative FusionSolar timestamp timezone.
    - `## Regression Commands`: list the targeted pytest commands from Plans 03-01 and 03-02.

    Do not edit application code in this plan. If the prior summaries document an auto-fixed provider bug, capture the final behavior and verification command here.
  </action>
  <verify>Select-String -Path .planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md -Pattern "FusionSolar collectTime","PVYield","Sigenergy rate-limit","Regression Commands"</verify>
  <done>The provider assumptions note exists and clearly separates pinned test behavior from unresolved external-provider assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Verify provider regression coverage and record the commands</name>
  <files>.planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md</files>
  <action>
    Run the targeted provider regression commands and update the note if command names or file names differ from the plan:
    - `python -m pytest -q tests/test_fusionsolar_provider_contracts.py tests/test_fusionsolar_service.py tests/test_fusionsolar_sync.py tests/test_performance.py tests/test_performance_backfill.py`
    - `python -m pytest -q tests/test_sigenergy_provider_contracts.py tests/test_scheduler_safety.py`

    Then run `python -m pytest -q tests/test_fusionsolar_provider_contracts.py tests/test_sigenergy_provider_contracts.py tests/test_scheduler_safety.py tests/test_performance_backfill.py` as the phase-level provider sanity command.

    If one targeted command fails because of a clear provider correctness bug introduced or exposed by Plans 03-01 or 03-02, fix the smallest affected test or helper under the original plan's ownership if still in that execution context; otherwise document the failure in the summary as a blocker. Do not broaden this documentation plan into an `app.py` refactor.
  </action>
  <verify>python -m pytest -q tests/test_fusionsolar_provider_contracts.py tests/test_sigenergy_provider_contracts.py tests/test_scheduler_safety.py tests/test_performance_backfill.py</verify>
  <done>The assumptions note lists the actual provider regression commands and the phase-level provider sanity command passes.</done>
</task>

</tasks>

<verification>
Run `python -m pytest -q tests/test_fusionsolar_provider_contracts.py tests/test_sigenergy_provider_contracts.py tests/test_scheduler_safety.py tests/test_performance_backfill.py`.
</verification>

<success_criteria>
- `.planning/phases/03-provider-integration-correctness/03-PROVIDER-ASSUMPTIONS.md` exists.
- The note references both FusionSolar and Sigenergy fixture-backed tests.
- Timestamp, unit, rate-limit, session/token expiry, and response-shape assumptions are explicit.
- The phase-level provider sanity pytest command passes or a blocker is documented in the plan summary.
</success_criteria>

<output>
After completion, create `.planning/phases/03-provider-integration-correctness/03-03-SUMMARY.md`.
</output>
