---
phase: 04-reporting-performance-and-alert-confidence
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app.py
  - tests/test_telegram_alert_policy.py
autonomous: true
requirements:
  - REQ-005
  - REQ-011
must_haves:
  truths:
    - "Repeated recovery events and repeated failure scans do not create Telegram notification spam."
    - "Persistent and recurrent alert cooldown behavior is pinned for sent, blocked, and repeated scans."
    - "FusionSolar API limit notification throttling remains compatible with the general Telegram alert audit table."
  artifacts:
    - path: "tests/test_telegram_alert_policy.py"
      provides: "Alert throttling, recovery, persistent, recurrent, and blocked-row regression coverage"
      contains: "process_monitoring_alerts"
    - path: "app.py"
      provides: "Telegram alert decision, persistence, and persistent/recurrent scan helpers"
      contains: "def alert_decision"
  key_links:
    - from: "tests/test_telegram_alert_policy.py"
      to: "app.py"
      via: "direct alert_decision and process_monitoring_alerts assertions"
      pattern: "alert_decision"
    - from: "tests/test_telegram_alert_policy.py"
      to: "telegram_alerts"
      via: "SQLite assertions for sent/blocked/failed rows"
      pattern: "telegram_alerts"
    - from: "app.py"
      to: "monitoring_board/services/telegram_service.py"
      via: "send_telegram_message monkeypatched in tests"
      pattern: "send_and_record_telegram_alert"
---

<objective>
Add regression coverage for Telegram throttling and notification edge cases around repeated failures, recoveries, persistent alerts, recurrent alerts, and provider API-limit notifications.

Purpose: Phase 4 needs confidence that operational alerts remain useful without spamming the operator when imports, syncs, or recoveries repeat.
Output: Focused alert policy tests plus only the smallest `app.py` correction if a new regression test exposes a clear spam-control bug.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-reporting-performance-and-alert-confidence/04-RESEARCH.md
@.planning/phases/03-provider-integration-correctness/03-VERIFICATION.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@app.py
@tests/test_telegram_alert_policy.py
@tests/test_performance_backfill.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover recovery and state-change cooldowns</name>
  <files>tests/test_telegram_alert_policy.py, app.py</files>
  <action>
    Extend `tests/test_telegram_alert_policy.py` using the existing `make_conn()`, `add_asset()`, and `configure_alerts()` helpers.

    Add tests that cover:
    - A `resolvido` alert sent at a fixed time blocks a second recovery event for the same asset within `RESOLVED_COOLDOWN_MINUTES`, even when the new event has a different `batch_id` and therefore a different alert key.
    - A `novo_erro` alert sent at a fixed time blocks another new-error event within `NEW_ERROR_COOLDOWN_MINUTES` when that setting is enabled.
    - Blocked rows are recorded with the expected `blocked_reason` and do not increment sent alert counts.

    Monkeypatch `send_telegram_message` to append messages to a list and return `True`; do not call Telegram. Use explicit `datetime(2026, 5, 14, ...)` values instead of `datetime.now()` for assertions.

    If a test exposes a spam-control bug, limit production changes to `alert_cooldown_active()`, `alert_decision()`, `process_monitoring_alerts()`, or directly related alert audit helpers.
  </action>
  <verify>python -m pytest -q tests/test_telegram_alert_policy.py -k "cooldown or recovery or novo_erro"</verify>
  <done>Recovery and new-error cooldowns are pinned across different batch IDs, with sent and blocked audit rows asserted in SQLite.</done>
</task>

<task type="auto">
  <name>Task 2: Cover persistent and recurrent alert repeats</name>
  <files>tests/test_telegram_alert_policy.py, app.py</files>
  <action>
    Add tests around `process_persistent_monitoring_alerts()`.

    Cover these cases:
    - A 24-hour persistent `Erro` scan sends one alert, and a repeated scan for the same still-open problem does not send a duplicate.
    - A two-hour persistent `Desconectada` scan respects `DAYTIME_OFFLINE_ONLY`; test one allowed daytime scan and one blocked night scan if the current helper supports it cleanly.
    - Recurrent seven-day alerts use the date-scoped key and `RECURRENT_COOLDOWN_HOURS`; with `SEND_RECURRENT_ALERTS=true`, a repeated scan on the same day should not send a duplicate.
    - Existing blocked rows for a still-open persistent/recurrent alert should not turn into extra sent rows during immediate repeated scans.

    Seed `monitoring_records` directly so `latest_monitoring_view` sees the latest problem state. Keep asset scope inside O&M and configure Telegram/env settings through existing helpers.

    If a test exposes a clear bug, fix only the affected alert key/cooldown/audit branch in `app.py`; do not redesign alert storage or add new tables.
  </action>
  <verify>python -m pytest -q tests/test_telegram_alert_policy.py -k "persistent or recurrent or Desconectada or Erro"</verify>
  <done>Persistent and recurrent alerts have deterministic regression coverage for repeated scans, cooldowns, sent rows, and blocked rows.</done>
</task>

<task type="auto">
  <name>Task 3: Pin provider API-limit alert throttling at the Telegram layer</name>
  <files>tests/test_telegram_alert_policy.py, app.py</files>
  <action>
    Add a focused test for `mark_fusionsolar_performance_rate_limited()` and `notify_fusionsolar_rate_limit()` in `tests/test_telegram_alert_policy.py`, even though broader backfill rate-limit coverage already exists.

    Assert that repeated calls inside the cooldown window:
    - update/preserve the FusionSolar cooldown app-state value,
    - create at most one `telegram_alerts.alert_type = 'fusionsolar_api_limit'` row,
    - do not call `send_telegram_message` more than once,
    - use existing `telegram_alerts` audit behavior rather than a separate notification store.

    Reset `app_module.FUSIONSOLAR_PERFORMANCE_RATE_LIMIT_UNTIL = None` before and after the test to avoid cross-test leakage.

    If a test exposes a bug, make the smallest change around `notify_fusionsolar_rate_limit()` or the cooldown-state helpers.
  </action>
  <verify>python -m pytest -q tests/test_telegram_alert_policy.py tests/test_performance_backfill.py -k "rate_limit or api_limit or alert"</verify>
  <done>FusionSolar API-limit notification throttling is pinned from the Telegram alert-policy test module and remains compatible with existing backfill coverage.</done>
</task>

</tasks>

<verification>
Run these commands:

`python -m pytest -q tests/test_telegram_alert_policy.py`

`python -m pytest -q tests/test_telegram_alert_policy.py tests/test_performance_backfill.py`
</verification>

<success_criteria>
- Repeated failures and recoveries produce at most the intended sent Telegram rows inside configured cooldown windows.
- Persistent and recurrent alert scans do not spam sent notifications when the same problem remains open.
- Blocked, failed, and sent alert audit rows are asserted through SQLite, not just mocked send-call counts.
- Any `app.py` behavior change is narrow, test-driven, and limited to alert policy or alert audit helpers.
- No live Telegram API calls are made during verification.
</success_criteria>

<output>
Commit after each completed task using exact path staging and messages like `test(04-02): add alert recovery cooldown regressions` or `fix(04-02): correct alert cooldown handling`.

After completion, create `.planning/phases/04-reporting-performance-and-alert-confidence/04-02-SUMMARY.md` with tests run, commits created, any auto-fixed bugs, and remaining risks.
</output>
