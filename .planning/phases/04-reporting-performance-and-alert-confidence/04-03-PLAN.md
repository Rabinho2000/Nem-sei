---
phase: 04-reporting-performance-and-alert-confidence
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - app.py
  - templates/exports.html
  - tests/test_exports.py
autonomous: true
requirements:
  - REQ-005
  - REQ-012
must_haves:
  truths:
    - "Customer production PDF route failures are user-visible through flash messages and redirects instead of raw 500 responses."
    - "FusionSolar API-limit failures during report generation mark cooldown state and remain visible to the export page."
    - "Generic PDF/XLSX export helpers have smoke and failure tests without writing generated files into the repository."
  artifacts:
    - path: "tests/test_exports.py"
      provides: "Route-level export/report failure and helper-level PDF/XLSX regression coverage"
      contains: "exports"
    - path: "app.py"
      provides: "Exports route, customer report builder, PDF generator, and generic export helper"
      contains: "def export_rows_file"
    - path: "templates/exports.html"
      provides: "User-visible export/report form and warning surface"
      contains: "fusionsolar_api_warning"
  key_links:
    - from: "tests/test_exports.py"
      to: "app.py"
      via: "authenticated Flask test client posts to /exports with CSRF"
      pattern: "build_fusionsolar_customer_production_report"
    - from: "app.py"
      to: "templates/exports.html"
      via: "GET /exports renders FusionSolar cooldown warning"
      pattern: "fusionsolar_api_warning"
    - from: "tests/test_exports.py"
      to: "app.py"
      via: "monkeypatched ReportLab/openpyxl boundaries for export_rows_file"
      pattern: "export_rows_file"
---

<objective>
Add focused PDF/XLSX export and customer report failure-handling coverage, then harden only the narrow route/helper gaps exposed by those tests.

Purpose: Phase 4 needs export/report failures to be visible and recoverable because these outputs drive operational and customer-facing decisions.
Output: A new `tests/test_exports.py` module plus only the smallest `app.py` or template correction if failure visibility is missing.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-reporting-performance-and-alert-confidence/04-RESEARCH.md
@.planning/phases/03-provider-integration-correctness/03-VERIFICATION.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@app.py
@templates/exports.html
@templates/base.html
@tests/test_performance.py
@tests/test_security.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add export helper smoke and failure tests</name>
  <files>tests/test_exports.py, app.py</files>
  <action>
    Create `tests/test_exports.py` with `from __future__ import annotations`.

    Add direct helper tests for `export_rows_file()` inside a Flask request context:
    - XLSX smoke test: pass one row and one or two headers, assert status code, XLSX mimetype, attachment filename, and non-empty response bytes. Do not write the workbook to disk.
    - PDF smoke test: pass one row and one or two headers, assert status code, PDF mimetype, attachment filename, and non-empty response bytes. Do not inspect PDF layout visually.
    - XLSX failure test: monkeypatch the workbook save boundary used by `export_rows_file()` to raise a deterministic exception and assert the exception propagates without creating files or mutating any SQLite state.
    - PDF failure test: monkeypatch the ReportLab document build boundary used by `export_rows_file()` to raise a deterministic exception and assert the exception propagates without creating files.

    Keep this task helper-level because generic export UI is not in current scope. Do not create a generic export route or UI.

    If monkeypatching the exact class boundary is awkward, patch the imported `Workbook` or `SimpleDocTemplate` symbol in `app.py` with a tiny fake that raises at `save()` or `build()`.
  </action>
  <verify>python -m pytest -q tests/test_exports.py -k "export_rows_file or xlsx or pdf"</verify>
  <done>`export_rows_file()` has deterministic PDF/XLSX smoke and failure coverage, and no generated export artifact is written to disk.</done>
</task>

<task type="auto">
  <name>Task 2: Cover customer report route failure visibility</name>
  <files>tests/test_exports.py, app.py, templates/exports.html</files>
  <action>
    Add authenticated Flask route tests for `/exports` in `tests/test_exports.py`.

    Use a `tmp_path` database with `ensure_database()`, temporarily override `flask_app.config["DATABASE"]`, and authenticate with `client.session_transaction()` including `csrf_token = "token"`. Restore the previous database config in `finally`.

    Cover these cases:
    - Invalid or missing `asset_id` POST redirects back to `/exports` and flashes the existing user-visible selection error.
    - If `build_fusionsolar_customer_production_report()` raises `ValueError("boom")`, the route catches it, flashes `Falha ao gerar relatorio de producao: boom`, and redirects back with `asset_id` and `report_month` query parameters.
    - If `export_customer_production_pdf()` raises after a valid report object is returned, the same route-level catch makes the failure user-visible and redirects instead of returning a 500.
    - For non-rate-limit failures, assert no FusionSolar cooldown app-state row is written.

    Follow redirects or inspect the session `_flashes` list to assert the flash category/message. Do not call live FusionSolar or generate real customer files.

    If a test exposes missing user visibility, make the smallest route/template fix in `app.py` or `templates/exports.html`.
  </action>
  <verify>python -m pytest -q tests/test_exports.py -k "exports and failure"</verify>
  <done>`/exports` failure paths redirect with user-visible errors and do not mutate cooldown state for non-rate-limit failures.</done>
</task>

<task type="auto">
  <name>Task 3: Cover report API-limit cooldown handling</name>
  <files>tests/test_exports.py, app.py, templates/exports.html</files>
  <action>
    Add route tests for the FusionSolar API-limit branch in `/exports`.

    Cover these cases:
    - When `build_fusionsolar_customer_production_report()` raises a recognized FusionSolar rate-limit exception such as `ValueError("failCode=407")`, the route calls `mark_fusionsolar_performance_rate_limited()`, commits cooldown app-state, flashes a warning, and redirects back to `/exports`.
    - A follow-up `GET /exports` renders the cooldown warning through the existing `fusionsolar_api_warning` context and `templates/exports.html`.
    - When the builder raises a pre-existing cooldown message containing `FusionSolar API temporariamente limitada`, the route flashes a warning and redirects without extending or duplicating the cooldown row.

    Use monkeypatches for report builder/PDF boundaries and direct SQLite assertions for `app_state`. Keep behavior aligned with existing rate-limit tests in `tests/test_performance_backfill.py`.

    If a test exposes a cooldown visibility bug, fix only the `/exports` rate-limit branch or the existing cooldown helper. Do not add audit tables or background report jobs in this phase.
  </action>
  <verify>python -m pytest -q tests/test_exports.py tests/test_performance_backfill.py -k "exports or rate_limit or cooldown"</verify>
  <done>Customer report rate-limit failures set/keep cooldown state, show a warning on `/exports`, and do not duplicate cooldown side effects.</done>
</task>

</tasks>

<verification>
Run these commands:

`python -m pytest -q tests/test_exports.py`

`python -m pytest -q tests/test_exports.py tests/test_performance.py tests/test_performance_backfill.py tests/test_security.py`
</verification>

<success_criteria>
- `tests/test_exports.py` exists and covers PDF/XLSX helper smoke paths plus controlled failure paths.
- `/exports` route failures for builder errors, PDF generation errors, invalid input, and FusionSolar rate limits are user-visible through flashes/redirects.
- Non-rate-limit export failures do not write cooldown state or corrupt data.
- No generic export UI is planned or built unless a current route requires it.
- No generated PDF/XLSX files, SQLite files, logs, uploads, or backups are committed.
</success_criteria>

<output>
Commit after each completed task using exact path staging and messages like `test(04-03): add export failure regressions` or `fix(04-03): harden export failure handling`.

After completion, create `.planning/phases/04-reporting-performance-and-alert-confidence/04-03-SUMMARY.md` with tests run, commits created, any auto-fixed bugs, and remaining risks.
</output>
