---
phase: 02-background-job-and-scheduler-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - tests/test_scheduler_safety.py
autonomous: true
requirements:
  - REQ-004
  - REQ-005
  - REQ-009
must_haves:
  truths:
    - "Repeated scheduler startup or refresh in one process does not duplicate recurring APScheduler jobs."
    - "FusionSolar and Sigenergy scheduled syncs are both registered with stable job IDs and single-instance cron options."
    - "Scheduled provider dispatch uses an explicit provider-neutral sync entry point and records failures through the existing sync-run path."
  artifacts:
    - path: "app.py"
      provides: "Scheduler lifecycle, recurring job registration, and scheduled integration dispatch"
      contains: "def refresh_integration_scheduler"
    - path: "app.py"
      provides: "Provider-neutral scheduled sync wrapper while preserving the existing run_fusionsolar_sync callable"
      contains: "def run_integration_sync"
    - path: "tests/test_scheduler_safety.py"
      provides: "Focused fake-scheduler and provider-dispatch regression tests"
      min_lines: 80
  key_links:
    - from: "app.py"
      to: "APScheduler"
      via: "refresh_integration_scheduler add_job calls"
      pattern: "replace_existing=True"
    - from: "app.py"
      to: "provider sync orchestration"
      via: "run_scheduled_integration_sync calls run_integration_sync with trigger_type='scheduled'"
      pattern: "run_integration_sync\\(conn, provider, trigger_type=\"scheduled\"\\)"
    - from: "tests/test_scheduler_safety.py"
      to: "app.py"
      via: "fake scheduler and monkeypatched provider sync"
      pattern: "refresh_integration_scheduler"
---

<objective>
Make recurring APScheduler registration deterministic and make scheduled provider dispatch explicit for both FusionSolar and Sigenergy.

Purpose: Phase 2 needs scheduled syncs that are easier to reason about and less likely to duplicate, overlap, or hide provider-specific failures.
Output: Narrow `app.py` scheduler/dispatch changes plus focused scheduler safety tests.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-background-job-and-scheduler-safety/02-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/01-runtime-artifact-and-deployment-hygiene/01-01-SUMMARY.md
@app.py
@tests/test_fusionsolar_sync.py
@tests/test_fusionsolar_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make recurring scheduler registration deterministic</name>
  <files>app.py, tests/test_scheduler_safety.py</files>
  <action>
    In `app.py`, keep the existing process-local `SCHEDULER is not None` startup guard and tighten recurring job registration inside `refresh_integration_scheduler()`.

    Add explicit APScheduler options to recurring provider sync jobs: `replace_existing=True`, `max_instances=1`, `coalesce=True`, and a conservative `misfire_grace_time` value such as 1800 seconds. Apply the same single-instance/coalescing options to `telegram-daily-summary` if that job remains registered through the same scheduler helper. Do not add multi-process locks, persistent job stores, Celery, Redis, or any new infrastructure.

    Add `tests/test_scheduler_safety.py` with a fake scheduler object that records `start()`, `get_jobs()`, `remove_job()`, and `add_job()` calls. Use a `tmp_path` SQLite database created with `ensure_database()`, insert enabled `integration_configs` rows for both `FusionSolar` and `Sigenergy` with `auto_sync_enabled=1`, assign the fake scheduler to `app_module.SCHEDULER`, and call `refresh_integration_scheduler(flask_app)` twice. Assert the resulting active job IDs stay unique and stable, include both providers, remove legacy `fusionsolar-sync-` IDs when present, and register provider jobs with the explicit APScheduler options above.
  </action>
  <verify>python -m pytest -q tests/test_scheduler_safety.py -k scheduler</verify>
  <done>Repeated refreshes leave one recurring job per provider/hour ID, no duplicate job IDs are present, and all recurring jobs have deterministic single-instance/coalescing options.</done>
</task>

<task type="auto">
  <name>Task 2: Route scheduled syncs through a provider-neutral entry point</name>
  <files>app.py, tests/test_scheduler_safety.py</files>
  <action>
    In `app.py`, add `run_integration_sync(conn, provider, trigger_type="manual")` as the provider-neutral sync entry point. Keep the existing `run_fusionsolar_sync()` function name intact for compatibility, and have the new wrapper delegate to it unless a smaller local refactor is clearly safer. Update `run_scheduled_integration_sync()` and `run_all_integration_syncs()` to call `run_integration_sync()` instead of calling `run_fusionsolar_sync()` directly.

    Keep the scheduled sync exception guard so APScheduler does not kill its thread on failures. Add concise provider-specific logging around scheduled start, success, and failure. Do not call real FusionSolar or Sigenergy APIs in tests.

    Extend `tests/test_scheduler_safety.py` to monkeypatch `run_integration_sync()` and call `run_scheduled_integration_sync(flask_app, INTEGRATION_PROVIDER_SIGENERGY)` against a `tmp_path` database. Assert the wrapper receives the Sigenergy provider and `trigger_type="scheduled"`. Add a companion assertion for `run_all_integration_syncs()` showing enabled providers dispatch through the same wrapper with the supplied trigger type. Where failure persistence is tested, use the existing sync-run path with monkeypatched provider checks and assert `integration_sync_runs.status='error'` and `integration_configs.last_error` rather than relying only on log output.
  </action>
  <verify>python -m pytest -q tests/test_scheduler_safety.py tests/test_fusionsolar_sync.py</verify>
  <done>Scheduled Sigenergy dispatch is explicitly covered, manual/all-provider sync dispatch still works through the compatibility wrapper, and provider sync failures remain visible in SQLite sync state.</done>
</task>

</tasks>

<verification>
Run `python -m pytest -q tests/test_scheduler_safety.py tests/test_fusionsolar_sync.py tests/test_fusionsolar_service.py`.
</verification>

<success_criteria>
- `refresh_integration_scheduler()` is idempotent under repeated calls with a fake scheduler.
- Recurring provider sync jobs use stable IDs, `replace_existing=True`, `max_instances=1`, `coalesce=True`, and explicit misfire behavior.
- `run_scheduled_integration_sync()` dispatches both FusionSolar and Sigenergy through `run_integration_sync(..., trigger_type="scheduled")`.
- No real external provider APIs, Telegram API, or manual UI checks are required for verification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-background-job-and-scheduler-safety/02-01-SUMMARY.md`.
</output>
