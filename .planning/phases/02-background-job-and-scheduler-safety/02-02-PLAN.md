---
phase: 02-background-job-and-scheduler-safety
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app.py
  - tests/test_background_job_recovery.py
autonomous: true
requirements:
  - REQ-004
  - REQ-005
  - REQ-009
must_haves:
  truths:
    - "Startup recovery marks stale running background jobs failed before rescheduling pending work."
    - "All pending persisted background jobs are considered for scheduler registration after restart, not only the first ten."
    - "Recovery reports how many jobs were stale, scheduled, or could not be scheduled without changing the deployment architecture."
  artifacts:
    - path: "app.py"
      provides: "Deterministic pending background job recovery and recovery summary/logging"
      contains: "def schedule_pending_background_jobs"
    - path: "app.py"
      provides: "Existing durable job failure state"
      contains: "def mark_stale_running_background_jobs_failed"
    - path: "tests/test_background_job_recovery.py"
      provides: "Focused tmp_path SQLite tests for startup recovery behavior"
      min_lines: 80
  key_links:
    - from: "app.py"
      to: "background_jobs table"
      via: "schedule_pending_background_jobs queries pending rows ordered by id"
      pattern: "WHERE status = 'pending'"
    - from: "app.py"
      to: "APScheduler date jobs"
      via: "schedule_pending_background_jobs calls schedule_background_job for each pending job id"
      pattern: "schedule_background_job\\(app,"
    - from: "tests/test_background_job_recovery.py"
      to: "app.py"
      via: "tmp_path database plus monkeypatched schedule_background_job"
      pattern: "schedule_pending_background_jobs"
---

<objective>
Make persisted background job startup recovery deterministic and visible without changing the in-process APScheduler plus SQLite architecture.

Purpose: Phase 2 needs pending and stale jobs to be easier to reason about after restarts so long-running production work does not silently stall.
Output: Narrow `app.py` recovery changes plus focused tmp SQLite tests for pending/stale recovery behavior.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-background-job-and-scheduler-safety/02-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/02-background-job-and-scheduler-safety/02-01-SUMMARY.md
@app.py
@tests/test_db_helpers.py
@tests/test_performance_debug.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover every pending background job on startup</name>
  <files>app.py, tests/test_background_job_recovery.py</files>
  <action>
    In `app.py`, update `schedule_pending_background_jobs(app)` so startup recovery selects all `background_jobs` rows with `status='pending'` ordered by ascending `id`. Remove the current hard `LIMIT 10`; do not replace it with another hidden cap. Keep stale-running recovery before pending selection.

    If useful for testability, add a small helper such as `fetch_pending_background_job_ids(conn) -> list[int]` beside the existing background job helpers. Keep the source of truth in SQLite and keep APScheduler as the trigger only. Do not mutate pending jobs to `running` during recovery; `run_background_job()` must remain responsible for the pending-to-running transition.

    Add `tests/test_background_job_recovery.py` using `tmp_path`, `ensure_database()`, and `get_db()`. Insert more than ten pending rows plus non-pending rows. Monkeypatch `schedule_background_job()` to collect scheduled IDs and return `True`. Assert every pending ID is scheduled exactly once in ascending order and success/failed/running rows are not scheduled.
  </action>
  <verify>python -m pytest -q tests/test_background_job_recovery.py -k pending</verify>
  <done>Restart recovery considers all pending rows deterministically and preserves the existing durable job state transition rules.</done>
</task>

<task type="auto">
  <name>Task 2: Return and log recovery outcomes</name>
  <files>app.py, tests/test_background_job_recovery.py</files>
  <action>
    Make `schedule_pending_background_jobs(app)` return a small summary dictionary while preserving existing callers that ignore the return value. Include at least: stale running jobs marked failed, pending jobs found, pending jobs successfully scheduled, and pending job IDs that could not be scheduled because `schedule_background_job()` returned `False`.

    Log recovery outcomes at concise levels: warning when stale running jobs are marked failed, info when pending jobs are scheduled, and error or warning when any pending job could not be registered. Keep `schedule_background_job()` responsible for the scheduler-not-running error message.

    Extend `tests/test_background_job_recovery.py` to insert one stale running job and one fresh running job, monkeypatch scheduling success/failure cases, call `schedule_pending_background_jobs()`, and assert the summary values. Assert the stale row is marked `failed` with a useful `error_message` and `finished_at`, the fresh running row remains `running`, and failed scheduling leaves the pending row pending for a future retry.
  </action>
  <verify>python -m pytest -q tests/test_background_job_recovery.py tests/test_db_helpers.py tests/test_performance_debug.py</verify>
  <done>Startup recovery has testable summary output, durable stale-job failure records, and no silent loss of pending work when APScheduler registration fails.</done>
</task>

</tasks>

<verification>
Run `python -m pytest -q tests/test_background_job_recovery.py tests/test_db_helpers.py tests/test_performance_debug.py`.
</verification>

<success_criteria>
- `schedule_pending_background_jobs()` marks stale running jobs failed before scheduling pending jobs.
- More than ten pending jobs are all scheduled after restart in deterministic `id` order.
- Failed scheduler registration is logged and reflected in a returned recovery summary without changing pending job rows to success or failed.
- Existing background job helper tests and request queueing tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-background-job-and-scheduler-safety/02-02-SUMMARY.md`.
</output>
